{% extends 'base.html.twig' %}

{% block body %}

    <div class="py-52 flex justify-center items-center">
        <h1 class="text-3xl">Bienvenu sur Daily Zer !</h1>
    </div>

    <div class="p-10 grid grid-cols-1 justify-items-center lg:grid-cols-4">
        {% for new in news %}
            <div class="p-2 my-2 h-52 w-full bg-gray-500 bg-opacity-50 rounded-md lg:w-11/12">
                <h3>{{ new.name }}</h3>
                <p>{{ new.shortDesc }}</p>
            </div>
        {% endfor %}
    <div>

{% endblock %}